# API Configuration
# NOTE: Do NOT set VITE_API_BASE_URL in development!
# In development, the app uses Vite proxy to avoid CORS issues.
# Only set this for production builds.
# VITE_API_BASE_URL=http://your-production-api.com/api/v1
