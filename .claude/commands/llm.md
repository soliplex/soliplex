Start the Ollama LLM server locally in the background.

Run the following command:
```bash
/opt/homebrew/bin/ollama serve
```

Run this in the background so I can continue working. Let me know when it's started and show me the initial output. If it's already running, that's fine - just let me know.