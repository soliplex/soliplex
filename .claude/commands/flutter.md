Start the Flutter frontend web app in the background.

Run the following command:
```bash
cd ../flutter && flutter run -d chrome --web-port 59001 --dart-define=DEFAULT_SERVER_URL=http://localhost:8000
```

Run this in the background so I can continue working. Let me know when it's started and show me the initial output.

**Note:** Flutter repo must be cloned at `../flutter` relative to this repo.
If not found, clone it: `git clone https://github.com/soliplex/flutter ../flutter`