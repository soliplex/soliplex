Start the Soliplex backend server in the background.

Run the following command:
```bash
source venv/bin/activate && OLLAMA_BASE_URL=http://127.0.0.1:11434 soliplex-cli serve example/minimal.yaml --no-auth-mode
```

Run this in the background so I can continue working. Let me know when it's started and show me the initial output.