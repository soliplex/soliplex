name: soliplex_client
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_ai_toolkit:
    git: https://github.com/enfold/flutter-ai.git
  flutter_appauth: ^9.0.1
  flutter_map: ^8.2.1
  flutter_markdown_plus:
    git: https://github.com/enfoldsystems/flutter_markdown_plus.git
  flutter_riverpod: ^2.6.1
  flutter_secure_storage: ^9.2.4
  geolocator: ^14.0.1
  go_router: ^16.0.0
  http: ^1.4.0
  json_annotation: ^4.9.0
  latlong2: ^0.9.1
  package_info_plus: ^9.0.0
  url_launcher: ^6.3.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  json_serializable: ^6.10.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
