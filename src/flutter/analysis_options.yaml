# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - "test/**"
    - "bin/**"
    - "**/*.g.dart"
    - "scripts/**"
  errors:
    # Ignore strict inference failures for existing code
    inference_failure_on_collection_literal: ignore
    inference_failure_on_instance_creation: ignore
    inference_failure_on_function_invocation: ignore
    inference_failure_on_untyped_parameter: ignore
    
    # Treat dynamic calls as info/ignore
    avoid_dynamic_calls: ignore
    
    # Ignore discarded futures if linter rule doesn't catch all
    discarded_futures: ignore
    extra_positional_arguments_could_be_named: ignore

linter:
  rules:
    public_member_api_docs: false
    lines_longer_than_80_chars: true
    
    # Allow cascaded invocations (preference)
    cascade_invocations: false
    
    # Allow discarded futures (common in tests/fire-and-forget)
    discarded_futures: false



# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options