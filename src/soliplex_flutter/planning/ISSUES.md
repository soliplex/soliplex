# Issues list

- [ ] In the history view, thread id and the date time created (if no activities there) or date time of the last activity should be displayed
- [ ] When different thread is clicked, the chat view should be updated to reflect the runs in the newly selected thread
- [ ] In the details view, remove the "Messages" tab, and add "Event log" (or "Event Log"? be consistent in the naming) and capture and display all events there. If there are same events streaming through multiple events (for example, if hundreds of thinking events coming in with parts of the text) just show 1 thinking event, and update its text as more thinking event comes in
- [ ] Add documentation for components of the app
