name: soliplex_frontend
description: Cross-platform Flutter frontend for Soliplex AI-powered RAG system
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.35.0'

dependencies:
  flutter:
    sdk: flutter

  # UI components
  flutter_highlight: ^0.7.0
  flutter_markdown: ^0.7.4+1

  # State management - Manual providers (NO generator)
  flutter_riverpod: ^3.0.0

  # Navigation
  go_router: ^17.0.0

  # HTTP client (for backend health checks)
  http: ^1.2.0

  # Utilities
  intl: ^0.20.1
  markdown: ^7.0.0
  meta: ^1.15.0

  # Local package - soliplex_client
  soliplex_client:
    path: packages/soliplex_client

  # Local package - soliplex_client_native
  soliplex_client_native:
    path: packages/soliplex_client_native

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Testing
  mocktail: ^1.0.4

  # Linting - Zero tolerance
  very_good_analysis: ^10.0.0

flutter:
  uses-material-design: true
